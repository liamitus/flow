// ------------------------------------------------------------------- Main code

(function (flow) {

    // Wait until the page loads to kick things off.
    window.onload = function() {

        flow.start();

    };

})(FLOW);
